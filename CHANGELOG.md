# Changelog

本檔案記錄 QQKey 的重要變更，格式依循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，
版本號依循 [Semantic Versioning](https://semver.org/lang/zh-TW/)。

## [Unreleased]

### Added

- **簡體中文介面**（`zh-Hans`），介面語言增為七個。候選框、設定畫面、系統匣選單、
  啟動失敗對話框與後端錯誤訊息都補上了這一份；內建目錄 104 筆命令的說明與搜尋
  關鍵字同樣七語言齊備。`keywords_all` 的聯集隨之擴為七語言，所以介面切成簡體
  之後，用繁體或其他語言的關鍵字一樣找得到命令，反之亦然。

### Changed

- **授權由 MIT 改為 GPL-3.0-only**。目的是 copyleft：散布衍生作品必須同樣以 GPL-3.0
  授權並附上原始碼，不能閉源再商業化。0.1.0 是以 MIT 發布的，那份授權不可撤回、
  仍然適用於當時的 commit，本次變更自此之後生效。`LICENSE`、兩份 README、
  `package.json`、`Cargo.toml` 與設定畫面的「關於」都同步更新。
- 系統語系判定改為區分簡繁。`zh` 是**唯一**需要再看 script 與 region 子標籤的語言
  （簡繁共用同一個主要子標籤），其餘語言仍然只看第一段。判別採白名單：明確指向
  簡體的（`Hans`／`CN`／`SG`／`MY`）對到 `zh-Hans`，其餘的 `zh` 一律 `zh-Hant`——
  含 `zh-TW`／`zh-HK`／`zh-MO` 與沒有子標籤的裸 `zh`。裸 `zh` 在 CLDR 的預設是簡體，
  這裡刻意不跟：Windows 的顯示語言從不回報這種形式，而把一個判不出來的標籤丟給
  繁中使用者看簡體，比反過來更像故障。此前所有 `zh` 開頭的標籤都對到繁體。

## [0.1.0] - 2026-07-30

### Added

- **M0**：`spike/caret-probe` caret 定位驗證工具，三層 fallback
  （`GetGUIThreadInfo` → UI Automation `TextPattern` → 視窗矩形）。
  實測確認 Windows Terminal 可經由 `TermControl` 元素精準取得游標座標。
- **M1**：Tauri v2 骨架，`launcher`（無邊框、透明、置頂）與 `settings` 雙視窗；
  全域快捷鍵 **Alt+Q** 叫出／收起候選框；失焦自動隱藏。
- **M1**：候選框 UI，支援 `↑↓` 移動、`1`–`9` 直選、`Enter` 填入、`Esc` 取消。
- **M1**：`spike/input-probe`，以 `SendInput` 自動驗證全域快捷鍵與文字注入可用性。
- **M2**：`inject.rs` 焦點還原（`SetForegroundWindow`，前景鎖定時以
  `AttachThreadInput` 繞過）與 `SendInput` Unicode 文字注入。
- **M2**：`template.rs` 佔位符截斷——送出的內容止於第一個 `{參數}` 之前，
  游標停在使用者該接手輸入的位置。附單元測試。
- **M3**：`caret.rs` 三層 fallback 取得游標座標，候選框貼齊輸入游標；
  含螢幕邊界夾制（下方放不下就翻到游標上方）與多螢幕支援，附 6 個單元測試。
- **M3**：以 `SetWinEventHook` 持續追蹤前景視窗，取代按下快捷鍵當下才查詢的作法。
- **M3**：收起候選框時把焦點還給原本的視窗，Esc 之後可直接接著打字。

- **M4**：SQLite 儲存（`store.rs`）與記憶體候選池（`state.rs`），
  候選池啟動時全載入，敲鍵搜尋不必碰資料庫。
- **M4**：內建命令目錄，涵蓋 usbipd、git、wsl、netsh、docker、winget、npm、cargo
  共 104 筆命令，全附繁體中文說明與中文搜尋關鍵字。
- **M4**：`ranking.rs` 以 `nucleo-matcher` 模糊比對，乘上 frecency 加權排序；
  frecency 採三十天半衰期的增量衰減，只需存分數與最後使用時間。
  分數相同時依 來源優先序 → 命令長度 決勝。
- **M4**：候選框改為向後端查詢真實資料；空查詢時只列出用過的命令，
  而不是塞一串沒用過的當噪音。

- **M5**：從 PSReadLine 歷史學習實際用過的命令。以檔案位移做增量匯入，
  只讀上次之後新增的部分；PSReadLine 重寫歷史檔導致檔案變短時自動從頭讀。
- **M5**：匯入前的機密過濾。命中 `password`、`token`、`secret`、`credential`、
  `ConvertTo-SecureString` 等樣式，或 `=`／`:` 後接 20 字元以上長字串的行整行略過。
  略過筆數會回報，內容不會被記錄。
- **M5**：歷史條目依出現次數換算初始 frecency（取對數壓縮），
  且不會覆寫內建目錄的同名條目，也不會把實際使用累積的分數回沖。
- **M5**：`meta` 資料表與 `import_history`／`history_import_enabled` 等 IPC，
  供 M6 的設定畫面使用。

- **M6**：設定畫面。命令字詞頁提供搜尋、來源篩選、分頁、新增／編輯／刪除、
  批次啟用停用、清除單筆使用統計；一般設定頁提供快捷鍵改綁、歷史學習開關與
  手動匯入、機密過濾規則編輯、以剪貼簿 JSON 分享自訂命令。
- **M6**：`Alt+Shift+Q` 開啟設定畫面。做成全域快捷鍵而非候選框裡的按鍵——
  中文輸入法會攔截 `Ctrl+,` 這類組合，把修飾鍵吃掉只留下一個全形逗號。
- **M6**：編輯過的內建條目會轉為 user 來源，之後同步內建目錄時不會被蓋回去。
- **M6**：匯出只含自訂條目。內建目錄對方也有，歷史學來的可能夾帶工作內容。

- **M7**：系統匣圖示與選單（叫出候選框／設定／結束）。左鍵單擊叫出候選框，
  選單上會顯示目前綁定的快捷鍵，改綁後提示文字同步更新。
- **M7**：開機自動啟動，於設定畫面開關。
- **M7**：關閉設定視窗改為收起而非銷毀——QQKey 是常駐工具，關窗不等於結束。
- **M7**：`npm run tauri build` 產出 MSI 與 NSIS 安裝檔。
- 應用程式圖示：發光的輸入游標貼著下方的候選框，取自產品本身的隱喻。
  來源檔為 `src-tauri/icons/source.png`，其餘尺寸由它產生。
- 候選框背景不透明度可調（20–100%，預設 92%），設定畫面提供滑桿與百分比輸入框，
  放手即套用。開著設定視窗時候選框是隱藏的，所以附一個疊在假終端機上的即時預覽——
  它用的是真的疊層與 `backdrop-filter`，所見即所得。
- 持久化日誌（`tauri-plugin-log`）與 panic hook。從前 `trace()` 在 release 版是空實作，
  而 `windows_subsystem = "windows"` 又讓幾處 `eprintln!` 寫進虛空——打包之後
  出問題手上什麼都沒有。日誌落在 `%LOCALAPPDATA%\com.jeremywen.qqkey\logs\`，
  設定畫面提供「開啟日誌資料夾」。**不記錄視窗標題，也不記錄注入內容。**
- 資料庫 schema 版本（SQLite `user_version`）與版本化 migration 階梯。
  版本比程式新時直接拒絕開啟並說明原因，不試著降級。
- single-instance 防護。開機自啟加上手動雙擊會跑出第二份：兩個系統匣圖示、
  兩份各自獨立的記憶體候選池，後啟動的那份還搶不到快捷鍵。改為叫出既有實例的候選框。

- 完整備份與還原（含檔案對話框）。`export_entries()` 刻意只匯出自訂條目，那對分享
  是對的，但它同時是唯一的資料出口——歷史學來的上千筆與累積的 frecency 換機器就歸零。
  備份帶走全來源、使用統計與整張 `meta`；還原是取代，包在同一個交易裡。
- 匯入前先試算並要求確認，講出會新增幾筆、覆寫幾筆。覆寫沒有 undo。
- 批次刪除，以及刪除前的確認對話框。從前清數千筆歷史雜訊得每頁 40 筆逐一點，
  而每一下都是沒有 undo 的硬刪除。另加「選取本頁」。
- 設定畫面顯示最後使用時間與手動加權。`lastUsed` 後端一直有傳，只是從未被畫出來。
- 候選框列表補上 `role="listbox"`／`option`／`aria-activedescendant`，
  分頁補 `role="tab"`，toast 補 `aria-live`。
- 兩個入口都包上 Error Boundary。候選框是無邊框透明視窗，React 拋錯留下的是一個
  看不見也關不掉的空視窗。
- 候選框底部的「Alt+Shift+Q 設定」變成可點擊——`open_settings` 這支 IPC 早就註冊好
  卻沒有任何人呼叫，滑鼠一直沒有辦法從候選框進到設定畫面。
- `rust-toolchain.toml` 釘住 1.92.0。`rusqlite` 固定 0.37 的理由與 toolchain 綁在一起，
  換機器時飄掉就會再踩一次，而錯誤訊息指向的是別人的 build script。
- 候選框 `Tab` 補完：把選取項目截至第一個佔位符的前綴填回搜尋框，內容與真正會
  送出的一致。補完不注入，接著可再縮小範圍或換成同系列的另一個命令。沒有候選時
  `Tab` 也一律吃掉——焦點跑到頁尾的設定按鈕上，後續打的字就不會進搜尋框，
  而畫面上看不出焦點在哪。
- 設定畫面新增「關於」頁：版本、授權、作者簡介與聯絡方式（Email、專案頁）。
  連結交由後端 `open_external` 轉給系統預設處理程式，只放行 `https://` 與
  `mailto:`——它收的是前端傳來的字串，而 explorer 拿到本機路徑會直接開檔案總管
  或執行關聯程式，不設限等於開了一道「開啟任意本機東西」的門。Email 另附
  「複製」，沒有註冊郵件軟體的機器上 `mailto:` 按下去不會有作用。
- **多語言支援**：繁體中文、日文、英文、法文、德文、韓文。首次啟動依 Windows
  **顯示語言**自動套用（Win32 `GetUserPreferredUILanguages`，而不是地區格式的
  `GetUserDefaultLocaleName`——顯示英文、地區台灣的機器用後者會被誤判成中文），
  設定畫面可手動覆寫，切換後立即生效：候選框、設定畫面、系統匣選單、設定視窗
  標題與內建命令的說明一次全換，不必重新啟動。
- 內建目錄 104 筆命令的說明與搜尋關鍵字全部六語言化。**關鍵字是六個語言的聯集**，
  所以介面切成英文之後，輸入「掛載」一樣找得到 `usbipd attach --wsl`——
  搜尋能力不隨介面語言縮小。
- 後端使用者可見字串（系統匣選單與提示、啟動失敗對話框、IPC 錯誤訊息）也一併
  六語言化。診斷日誌刻意維持單一語言：讀者是開發者，固定語言才 grep 得到，
  而且同一份日誌不該前後兩種語言。

### Changed

- 候選框不再撐滿整個視窗，只佔用實際需要的高度。查無結果時原本是一個 640×420
  的大方塊浮在游標旁，把使用者正在看的終端機內容遮掉一大片。
- 候選框顯示的 ★ 分數與設定畫面的分數欄改為**衰減後**的值，跟排序用的是同一個。
  從前顯示原始累計值，於是三個月沒碰的命令標著 ★10 卻排在 ★3 後面。
- 候選框直選鍵由 `1`–`9` 改為 `Alt+1`–`Alt+9`。命令名稱本身就常帶數字
  （`7z`、`base64`、`python3`），裸數字當選取鍵的話這些字串永遠打不進查詢；
  而且原本的 `preventDefault` 只在候選數夠多時才執行，同一顆鍵會時而選取時而輸入。
- 單筆「停用／啟用」改走 `set_entries_enabled`，與批次同一條路。
- `entry` 表新增 `keywords_all` 欄位存放六語言關鍵字聯集，`SCHEMA_VERSION` 1 → 2。
  與 `keywords` 分開是因為後者是設定畫面「搜尋關鍵字」欄位讀的那一份：把六語言塞
  進去，使用者一按存檔就把那團字變成自己的關鍵字。編輯或匯入條目時 `keywords_all`
  會被清空——使用者接手之後就只吃他填的字。
- 設定畫面的相對時間（「3 天前」）改用 `Intl.RelativeTimeFormat`，分數欄改用
  `Intl.NumberFormat`。後者讓德文與法文的小數點正確顯示為逗號。
- 歷史匯入統計、條目筆數與分頁、匯入預覽的訊息改寫成完整句子，不再用「前半句
  加上一個以逗號開頭的續段」或字串串接——那種寫法假設了語序，也不讓譯者改標點。
  順帶修掉一個中文下也存在的問題：掃描 0 行時原本會印「掃描 0 行，匯入 0 筆，
  略過 0 筆…（沒有新增的歷史紀錄）」，同一件事講兩遍。
- CSS 字型堆疊移除具名 CJK 字型，改由 Chromium 依 `<html lang>` 做語言感知
  fallback。留著繁中字型會讓日文使用者拿到中文字形（直、次、骨、令 在兩個語言
  下不同），韓文諺文則兩者都沒有。
- 設定畫面的「關於」列表改用 CSS grid，欄位名不再固定 68px——那個寬度只在中文下
  成立，德文的 Projektseite 與日文的「プロジェクトページ」都會被截掉。
  候選框頁尾與工具列加上 `flex-wrap`，讓德文的長按鈕文字換行而不是把輸入框壓扁。

### Fixed

- **M3**：`settings` 視窗即使設為 `visible: false`，在啟動時仍會被 Windows
  當成前景視窗，害候選框把它誤認為注入目標。改為需要時才動態建立。
- **M3**：`tauri.conf.json` 的 `"center": true` 會在顯示時覆寫算好的座標，已移除；
  改為定位失敗時才主動置中。
- **M6**：候選框顯示後輸入框拿不到鍵盤焦點。視窗有焦點不代表 webview 有，
  webview 有也不代表輸入框有——三層都要點名，且要等下一幀再做。
- **M6**：移除前景追蹤 hook 的診斷輸出。它每次切換視窗都會觸發，
  等於把使用者一整天開過哪些視窗的標題全寫進 log。
- 候選框在中文輸入法組字期間會誤觸發。注音選字的 `Enter` 會直接把命令注入出去、
  `↑↓` 會連動改變候選選取——而搜尋本來就鼓勵打中文關鍵字（輸入「掛載」找
  `usbipd attach`）。改為組字期間不處理按鍵。
- 注入失敗完全無回饋。候選框在注入**之前**就收起，失敗訊息只進 console，
  而 release 版沒有 console；以系統管理員身分開的終端機會擋下 `SendInput`，
  那正是 usbipd 這類命令的日常情境。改為失敗時把候選框叫回來並顯示原因。
- 注入前未過濾控制字元。`SendInput` 逐字送出，`\r\n` 到終端機就等同 Enter——
  命令會直接執行，`\t` 則觸發 PowerShell 補全改寫已填入的文字。而樣板內容
  不全是手打的：剪貼簿匯入的 JSON 完全繞過設定畫面的單行輸入框。
  現在入口擋、注入前再濾一次。
- 資料庫開不起來時程序靜默結束，使用者按下捷徑只覺得什麼都沒發生。
  改為跳系統對話框說明原因與資料庫路徑。
- 快捷鍵註冊失敗退回預設後狀態不一致。設定值沒被更新，於是系統匣與設定畫面
  顯示的是一個按了沒反應的組合；更糟的是後續換綁會去解除那個從未註冊成功的
  組合，退回註冊的 `Alt+Q` 從此賴在系統裡，使用者再也設不回預設值。
  改為區分「設定值」與「實際生效」，換綁一律以後者為解除目標。
- 單筆「停用」會把內建條目靜默轉成自訂——它從此收不到內建目錄更新，
  還會被算進分享檔，與「只含自己新增或編輯過的」不符。批次停用則不會，
  兩顆看起來一樣的按鈕後果不同。
- 匯入分享檔會覆寫 `enabled`，把你刻意停用的條目重新打開。內容照常更新，
  但啟用狀態保留。
- 手動加權填負數會讓排序權重的 `ln()` 得出 NaN，而 NaN 在比較時被 `partial_cmp`
  判為 `None` 吞掉——那筆會卡在原始順序上且完全不受查詢相關度影響，症狀隱晦到
  幾乎不可能歸因。入口拒絕負值，排序端也對既有資料夾制。
- 勾選狀態跨頁殘留。在第 1 頁勾了十筆、翻到第 2 頁後批次列仍寫著「已選取 10 筆」，
  按下停用會作用在螢幕上看不見的條目。改為翻頁即清空選取。
- 儲存失敗會吞掉使用者剛打完的內容——對話框在 `await` 之前就關閉，撞上 UNIQUE
  約束時整筆重打。改為成功才關閉。
- 一般設定載入失敗會永久卡在「載入中…」，而唯一的錯誤 toast 四秒後就消失。
  現在會顯示原因並給出重試。
- 設定視窗關閉只是 `hide()`，重新開啟看到的是上次的舊資料。改為重新取得焦點時
  再讀一次。
- 候選框搜尋失敗與「查無結果」長得一模一樣，使用者只會一直改關鍵字重試。
- 長命令會把說明與分數擠出候選框後被硬切，沒有省略號也看不出被截斷。
- 候選框沒有 `scrollIntoView`，目前九筆剛好塞得下純屬巧合——列數一多或字型一大，
  鍵盤選取就會移出視野而畫面毫無反應。
- 編輯對話框無法用 Esc 關閉、Tab 會跑到底下的表格按鈕上、點遮罩即丟棄編輯內容。
- 改綁快捷鍵後系統匣**選單文字**不更新。原本只有 tooltip 會刷新，於是改綁成
  `Alt+W` 之後右鍵選單照樣寫著「叫出候選框（Alt+Q）」。現在選單與 tooltip 走
  同一支 `tray::refresh()`。
- 從備份還原後不推送任何副作用。`restore()` 會覆寫整張 `meta`，於是還原進來的
  設定（含介面語言）已經生效，但系統匣、視窗標題與兩個視窗都還記著舊的，
  要到重新啟動才對得上。
- 候選框的搜尋失敗訊息顯示成「填不進去：搜尋失敗：…」。兩種失敗共用同一個
  state，而 JSX 那端固定加上「填不進去：」前綴。改為兩者各自把話講完整。

### Notes

- `rusqlite` 固定在 0.37：0.40 會帶進 `libsqlite3-sys` 0.38，其 build script
  用了 unstable 的 `cfg_select`，在 Rust 1.92 stable 上編不過。
- 重新啟動 QQKey 時，前一個程序要完全結束後全域快捷鍵才會釋放。
  太快啟動新的會註冊失敗——現在這件事會寫進日誌，設定畫面也會標示出
  「設定的組合沒有生效」。
